name: Release

on:
  workflow_dispatch:

jobs:
  product:
    uses: ./.github/workflows/reusable-image.yml
    with:
      service: product-service
    secrets: inherit

  user:
    uses: ./.github/workflows/reusable-image.yml
    with:
      service: user-service
    secrets: inherit